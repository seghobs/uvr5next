{
  "name": "uvr5-ui-root",
  "version": "1.8.4",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n \"BACKEND,FRONTEND\" -c \"cyan.bold,magenta.bold\" \"node start_backend.js\" \"npm --prefix frontend run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
